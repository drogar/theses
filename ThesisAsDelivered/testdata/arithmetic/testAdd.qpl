#Import adder.qpl

main::()=
{
   a0 = |0>;
   Had a0;
   a1 = |0>;
   Not a1 <= a0;
   b0 = |1>;
   b1 = |0>;
   ain = Cons(a0,Cons(a1,Nil));
   bin = Cons(b0,Cons(b1,Nil));
   c0 = |0>;
   adder c0 ain bin;
}